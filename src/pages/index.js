import React from 'react';

const Home = () => {
    return ( <
        div style = {
            {
                display: 'flex',
                justifyContent: 'Left',
                alignItems: 'Left',
                height: '100vh'
            }
        } >
        <
        h1 > Welcome to OpenPool.io, make sure you 're always on the correct link</h1> <
        /div>
    );
};

export default Home;